# 摊主日记账：开发计划与架构规范

> 文档状态：`v0.7`
> 最后更新：2026-08-14  
> 适用范围：本仓库内的产品、开发、测试、部署及多 Agent 协作。  
> 本文档是项目当前的单一事实来源；范围或架构变更时，先更新本文档，再开始实现。

## 1. 产品定位

为线下摆摊、流动经营和微型商户提供一款**手机优先的日常收银记录工具**。商家可在几秒内记录收入和支出，并随时查看今天、本周、本月的经营数据。

核心原则：

- **快**：单笔收支录入不超过 3 个主要操作。
- **稳**：网络不稳定时仍可录入；恢复网络后自动同步。
- **清楚**：首页优先呈现收入、支出、净收益和收款构成。
- **安全**：每位商家只能访问自己的数据；金额、账目和备份可追溯。
- **克制**：第一版先解决日记账与经营看板，不做完整进销存或支付系统。

## 2. 目标用户与关键场景

| 用户 | 场景 | 需要完成的动作 |
| --- | --- | --- |
| 单人摊主 | 营业中快速补记一笔收款 | 选择收入、收款方式、金额后保存 |
| 单人摊主 | 收摊时核对现金 | 查看现金应有金额，录入实点金额并确认差额 |
| 小店老板 | 想知道近期经营是否变好 | 查看日/周/月趋势、支付方式占比与净收益 |
| 家庭商户所有者 | 全家经营多个摊位 | 查看全部或单个摊位的数据，邀请家人并分配权限 |
| 家庭成员 / 合伙人 | 负责一个或多个摊位 | 仅查看或记录被授权摊位的账目 |

## 3. MVP 范围（第一版必须完成）

### 3.1 功能清单

1. **账户、家庭商户与摊位**
   - 注册、登录、退出；初始版本使用账号密码登录，手机号短信验证作为后续增强。
   - 新用户创建一个默认家庭商户和默认摊位；摊位数据归家庭商户，不归某个个人账号。
   - 所有者可邀请家人或合伙人加入，并为其授权全部摊位或指定摊位；第一版仅所有者可管理成员，防止成员自行扩大权限。
   - 支持四种角色：所有者、管理员、记账员、查看者；具体权限见第 5 节。
2. **今日记账**
   - 新增收入或支出：金额、发生时间、收款/付款方式、分类、备注。
   - 支持现金、微信、支付宝、银行卡、其他五种方式。
   - 可编辑、删除、按日期筛选账目。
3. **日结与对账**
   - 记录开档备用金、实点现金、收摊备注。
   - 自动计算应有现金：`开档备用金 + 现金收入 - 现金支出`。
   - 显示实点差额，但不阻止商家完成日结。
4. **经营看板**
   - 今日收入、今日支出、今日净收益、今日交易笔数。
   - 最近 7 天收入/净收益趋势图。
   - 本月收入、支出、净收益汇总。
   - 今日收款方式占比与最近账目列表。
   - 有多个摊位时，可切换“全部摊位”与任一被授权摊位；全部摊位数据为已授权摊位的聚合。
5. **移动端与数据可靠性**
   - 响应式网页，优先适配 360–430px 宽的手机。
   - 支持安装为 PWA；已访问页面和待同步账目可离线使用。
   - 离线操作先保存在本机队列，联网后自动同步；用户能看到同步状态。
6. **数据导出与基础设置**
   - 按时间范围导出 CSV。
   - 设置默认货币、时区、摊位名称和常用收支分类。

### 3.2 明确不在 MVP 中

- 对接微信支付、支付宝或银行卡的真实资金流水。
- 库存、采购、供应商、会员、营销、外卖、电子发票和打印机。
- 复杂审批、完整会计报表、AI 经营建议。
- 多商家 SaaS 计费与订阅。

任何新增功能必须先判断是否直接提高“快速记账”或“看懂经营数据”的能力；否则放入后续版本。

## 4. 信息架构与页面

| 路由 | 页面目标 | 主要内容 |
| --- | --- | --- |
| `/login` | 进入系统 | 登录、注册、找回密码入口 |
| `/onboarding` | 首次设置 | 家庭商户名、默认摊位名、货币、常用分类 |
| `/today` | 营业中的主页面 | 当前摊位选择、今日指标、快速记一笔、最近账目、日结入口 |
| `/records` | 查账与修正 | 摊位范围与日期筛选、账目列表、新增/编辑/删除 |
| `/analytics` | 经营数据展示 | 单摊位/全部摊位切换、7 天趋势、月度汇总、收款方式占比 |
| `/settings` | 管理数据与偏好 | 家庭商户、摊位、成员权限、分类、导出、账户与同步状态 |

手机端固定底部导航为：`今日`、`记一笔`、`账目`、`数据`、`我的`。其中“记一笔”是最醒目的主操作。

## 5. 数据与业务规则

### 5.1 核心实体

| 实体 | 关键字段 | 说明 |
| --- | --- | --- |
| `users` | `id`、`email`、`password_hash`、`created_at` | 登录主体；可在后续增加手机号 |
| `organizations` | `id`、`name`、`currency`、`timezone`、`created_at` | 家庭商户 / 合伙商户，是摊位与经营数据的归属主体 |
| `organization_members` | `organization_id`、`user_id`、`role`、`status`、`joined_at` | 家庭成员或合伙人；角色为 `owner`、`admin`、`bookkeeper`、`viewer` |
| `stores` | `id`、`organization_id`、`name`、`timezone` | 摊位/门店，归属于家庭商户；不再直接归属于用户 |
| `member_store_permissions` | `member_id`、`store_id`、`access_level` | 指定摊位授权；所有者默认拥有全部摊位权限，其他角色必须有明确授权 |
| `ledger_entries` | `id`、`store_id`、`type`、`amount_fen`、`payment_method`、`category`、`occurred_at`、`note` | 一笔收入或支出的事实来源 |
| `daily_closings` | `id`、`store_id`、`business_date`、`opening_cash_fen`、`actual_closing_cash_fen`、`status` | 每日营业与现金对账状态 |
| `sync_operations` | `id`、`store_id`、`client_operation_id`、`status`、`processed_at` | 离线同步的幂等记录 |
| `audit_logs`（后续） | `actor_id`、`action`、`target_id`、`created_at` | 关键数据变更追踪 |

### 5.2 不可违反的规则

- 金额统一使用整数分 `amount_fen` 保存和计算，展示时再格式化为元；禁止用浮点数处理金额。
- `ledger_entries.type` 只能是 `income` 或 `expense`；金额必须大于 0。
- 摊位数据属于 `organizations`，不属于个人账号；成员离开后历史账目必须保留在该家庭商户名下。
- 所有查询和写入都必须校验：当前用户是该 `organization` 的有效成员，且拥有目标 `store` 所需的访问级别；不能只依赖前端隐藏数据。
- `owner` 可管理全部摊位、成员、设置和导出；`admin` 可管理获授权摊位及其账目，但不能管理成员；`bookkeeper` 可查看并记录获授权摊位账目；`viewer` 仅可查看获授权数据。
- 邀请仅在接受后生效；移除成员只撤销后续访问，不删除其历史操作和账目。
- “全部摊位”汇总只聚合当前用户有权访问的摊位，不能因汇总接口泄露未授权摊位数据。

### 5.3 角色与摊位范围矩阵

| 角色 | 默认可访问范围 | 允许的操作 | 不允许的操作 |
| --- | --- | --- | --- |
| `owner` | 该家庭商户的全部摊位 | 管理摊位、账目、日结、导出、成员邀请与移除 | 无法访问其他家庭商户 |
| `admin` | 仅被明确授权的摊位 | 管理获授权摊位的账目、日结和基础设置 | 邀请/移除成员、修改角色、访问未授权摊位 |
| `bookkeeper` | 仅被明确授权的摊位 | 新增、查看和编辑本人获授权摊位的账目与日结 | 删除已同步账目、导出、管理成员或摊位设置 |
| `viewer` | 仅被明确授权的摊位 | 查看账目和数据看板 | 新增、编辑、删除、导出、日结或设置 |

“授权全部摊位”在实现上应为该成员对当前所有摊位拥有授权，并在新增摊位时由所有者选择是否自动继承；MVP 默认**不自动继承**，避免新开摊位的数据被意外共享。
- 同一摊位、同一营业日期最多只有一条 `daily_closings` 记录。
- 收入、支出和净收益由账目聚合得出；不得把看板汇总值当作唯一数据来源。
- 删除采用用户可见的软删除或审计记录；不能静默抹去已同步账目。
- 所有“今天”与“营业日”均按该摊位设置的时区计算，默认 `Asia/Shanghai`。

## 6. 技术架构

第一版采用**模块化单体应用**：部署简单、团队协作边界清晰，后续可按模块拆分，不提前引入微服务复杂度。

```text
手机浏览器 / PWA
  ├─ Next.js + React + TypeScript 用户界面
  ├─ IndexedDB：离线账目与同步队列
  └─ Service Worker：静态资源和最近页面缓存
            │ HTTPS / JSON API
            ▼
Next.js 服务端（Route Handlers）
  ├─ 身份认证、组织成员与摊位访问控制
  ├─ 记账、日结、数据分析 API
  ├─ 输入校验与同步幂等处理
  └─ Prisma 数据访问层
            │
            ▼
PostgreSQL
  ├─ 用户、门店、账目、日结
  └─ 定时备份与恢复演练
```

### 6.1 推荐技术栈

| 层级 | 选择 | 原因 |
| --- | --- | --- |
| 应用框架 | Next.js + React + TypeScript | 同一项目完成前端、API 与服务端渲染，开发成本低 |
| UI | Tailwind CSS + 统一组件库 | 快速实现清晰的大触控界面 |
| 数据库 | PostgreSQL | 适合可靠事务、聚合分析与后续多门店扩展 |
| ORM 与迁移 | Prisma | 数据模型、迁移和类型检查集中管理 |
| 表单校验 | Zod | 客户端与服务端共享校验规则 |
| 图表 | Recharts | 适合趋势图和占比图，移动端易定制 |
| 离线存储 | IndexedDB（建议 Dexie 封装） | 容量和查询能力优于 localStorage |
| PWA | Service Worker + Web App Manifest | 支持添加到手机桌面和弱网可用 |
| 测试 | Vitest、React Testing Library、Playwright | 覆盖逻辑、界面和关键用户流程 |

### 6.2 离线与同步策略

1. 用户新增或修改账目时，界面立即显示结果，同时写入 IndexedDB 的本地账目和待同步队列。
2. 每条同步操作由客户端生成全局唯一的 `client_operation_id`。
3. 网络恢复、应用重新打开或用户主动点击同步时，按时间顺序提交队列。
4. 服务端以 `client_operation_id` 做幂等校验；重复请求必须返回原结果，不能重复记账。
5. 同步成功后更新本地状态；失败时保留队列并显示可理解的错误原因。
6. 首页缓存最近一次成功同步的看板数据，但明确标识“数据更新时间”。

MVP 不处理多设备同时编辑同一笔账目的复杂冲突；第一版采用“最后一次成功提交的修改生效”，并保留更新时间。若真实用户需要，第二版再引入版本号和冲突提示。

## 7. 接口边界（MVP）

| 方法与路径 | 职责 |
| --- | --- |
| `POST /api/auth/register` | 创建账户、默认家庭商户与默认摊位 |
| `POST /api/auth/login` | 建立登录会话 |
| `GET /api/organizations/current` | 获取当前家庭商户、成员角色与可访问摊位 |
| `GET /api/stores` | 获取当前成员可访问的摊位列表 |
| `POST /api/organizations/current/invitations` | 邀请成员，并设定角色与摊位范围（仅所有者） |
| `PATCH /api/organization-members/:id` | 调整成员角色、摊位授权或移除成员（仅所有者） |
| `GET /api/ledger-entries` | 按日期、类型、分类查询账目 |
| `POST /api/ledger-entries` | 新建收入或支出，支持同步操作 ID |
| `PATCH /api/ledger-entries/:id` | 修改账目 |
| `DELETE /api/ledger-entries/:id` | 软删除账目 |
| `GET /api/daily-closing/:date` | 获取某日营业和现金对账 |
| `PUT /api/daily-closing/:date` | 保存或更新日结 |
| `GET /api/analytics/summary` | 返回当前成员有权访问的指定摊位或全部摊位的汇总、趋势与占比 |
| `GET /api/exports/ledger.csv` | 导出当前成员有权访问范围内的账目 CSV |

所有写入接口均需要：认证、门店归属校验、字段校验、金额范围校验、结构化错误码和请求日志。

## 8. 代码结构约定

```text
src/
  app/                 # 页面与 API 路由
  components/          # 跨模块通用 UI 组件
  features/
    auth/              # 登录、会话、权限
    ledger/            # 账目领域
    closing/           # 日结与现金对账
    analytics/         # 看板、聚合、图表
    organizations/     # 家庭商户、成员邀请、角色与摊位授权
    stores/            # 摊位与分类设置
    sync/              # IndexedDB、离线队列、同步状态
  lib/                 # 数据库、校验、日期、金额等基础工具
  styles/
prisma/                # 数据模型与迁移
public/                # 图标、manifest、静态资源
tests/
  unit/
  integration/
  e2e/
```

约定：领域逻辑放在 `features/<领域>`，不能把业务计算散落在页面组件中；金额、日期、权限和接口校验使用共享工具，禁止重复实现。

## 9. 分阶段任务与验收标准

| 阶段 | 任务 | 完成标准 | 依赖 |
| --- | --- | --- | --- |
| P0：产品基线 | 确认字段、页面原型、本文档 | MVP 范围、术语和验收标准均已确认 | 无 |
| P1：工程基座 | 初始化项目、代码规范、环境变量、组织/成员/摊位迁移、认证与角色授权骨架 | 新成员可按 README 启动，角色矩阵对应的权限测试可运行 | P0 |
| P2：每日记账 | 账目 CRUD、分类、金额与日期校验、今日列表 | 手机端可完整新增、编辑、删除一笔账目 | P1 |
| P3：日结与看板 | 今日汇总、现金对账、单/全部摊位切换、7 天趋势、月度汇总 | 聚合数值与测试数据一致，空数据状态友好 | P2 |
| P4：移动与离线 | PWA、IndexedDB 队列、同步、同步状态 | 断网新增账目后恢复网络，服务端仅产生一笔记录 | P2 |
| P5：质量与安全 | 自动化测试、成员权限测试、错误处理、导出与备份 | 关键 E2E 流程通过，不存在跨组织或越权摊位访问 | P2–P4 |
| P6：发布试用 | 预发布环境、域名、HTTPS、监控、试用反馈 | 手机可安装访问，试用商家可连续日常记账 | P5 |

### P2/P3 必测业务样例

- 收入 25.50 元、支出 4.80 元时，净收益显示 20.70 元。
- 开档现金 100 元、现金收入 220 元、现金支出 30 元时，应有现金为 290 元。
- 删除一笔收入后，今日、周趋势和月度汇总应同时更新。
- 空账目、无网络、同步失败、无权限访问和重复同步请求均有明确界面或错误响应。
- 所有者可看两个摊位的合计与单摊位数据；记账员无法查询、导出或修改未被授权摊位的数据。

## 10. 多 Agent 协作规范

### 10.1 任务状态与交接

状态只能按以下顺序流转：`TODO → IN_PROGRESS → REVIEW → DONE`；遇到外部依赖或无法判断的产品决策时标记为 `BLOCKED` 并写明原因。

每个任务开始前必须记录以下信息（在 Issue、任务卡或协作消息中）：

```text
任务 ID：P2-LEDGER-01
目标：实现账目新增与金额校验
范围：src/features/ledger、src/app/api/ledger-entries
不包含：日结、图表、离线同步
依赖：P1-DB-01
验收：新增一笔收入/支出后列表和今日汇总正确；测试通过
验证命令：<由实现 Agent 补充>
```

交接时必须说明：已改文件、验证结果、未解决问题、后续 Agent 的首个动作。没有验证结果的任务不得标记 `DONE`。

### 10.2 文件与边界

- 同一时间同一文件只能由一个 Agent 主导修改；需要协作时先约定拆分点。
- Agent 不得顺手重构无关模块，也不得修改未声明的环境、部署或权限配置。
- 数据库 schema、迁移、认证、组织权限和共享类型属于高冲突区域：修改前必须声明，修改后要求至少一名其他 Agent 审查。
- 新增依赖必须说明用途、体积/维护风险和替代方案；未经确认不得引入付费 SaaS、真实支付 SDK 或收集额外个人信息。
- 不执行批量或递归删除；删除单个明确文件前先确认它不再被引用。

### 10.3 代码与测试规范

- TypeScript 开启严格模式；对外 API 参数和返回值必须有类型与 Zod 校验。
- 业务金额只能用“分”的整数和专用金额工具处理。
- 新功能至少包含相应的单元测试；影响关键流程时补充 E2E 测试。
- 所有用户可见文案使用简体中文；错误信息应告诉用户下一步可做什么。
- 提交实现前依次运行：类型检查、相关单元测试、格式检查；影响登录/记账/同步时运行对应 E2E。
- 禁止在代码、文档、测试或截图中写入密钥、真实手机号、真实账目或生产数据库连接串。

### 10.4 审查清单

- 是否只实现了任务范围内的内容？
- 任意金额计算是否避免了浮点误差？
- 任意接口是否完成认证、组织成员资格与摊位授权校验？
- 修改/删除账目后，所有相关统计是否一致？
- 手机窄屏、加载中、空数据、失败和离线状态是否可用？
- 是否有测试或可重复验证步骤？

## 11. 安全、隐私与运维

- 使用 HTTPS；会话 Cookie 设置 `HttpOnly`、`Secure`、`SameSite`。
- 密码只能存安全哈希；登录接口应限流，服务端密钥仅放在环境变量中。
- 数据库按最小权限连接；生产环境开启自动备份，并定期做恢复演练。
- 账目属于经营敏感数据，导出文件由用户主动生成；日志不得记录完整账目备注、密码或令牌。
- 首次正式上线前准备隐私政策、数据删除流程和用户反馈渠道。
- 若生产服务器部署在中国大陆，需在上线计划中安排域名与 ICP 备案；若提供收费在线服务、真实支付或更广泛的数据处理，发布前进行专项合规评估。

## 12. 上线策略与待决策项

### 12.1 环境

| 环境 | 用途 | 数据规则 |
| --- | --- | --- |
| 本地开发 | 功能开发与自动测试 | 仅使用假数据和本地数据库 |
| 预发布 | 集成验证与邀请试用 | 与生产隔离，可重置测试数据 |
| 生产 | 真实商家使用 | 备份、监控、最小权限、严格环境变量 |

### 12.2 域名与发布

- 开发和小范围试用可先使用托管平台提供的 HTTPS 测试域名。
- 对外正式运营前注册独立域名，并绑定生产环境；同时设置监控、错误告警、备份与数据导出。
- PWA 需要 HTTPS 才能稳定提供安装和离线能力；独立域名不是开发前置条件，但应是正式发布前置条件。

### 12.3 需要产品负责人确认的事项

1. 第一批试用商家是否只在中国大陆使用？这决定部署区域、备案节奏和访问优化。
2. MVP 是否仅做“手工录入总额/单笔收支”，还是提供商品级快速点单？建议前者。
3. 是否需要区分微信与支付宝的“实际到账”和“展示金额”？建议 MVP 先按手工录入的实际金额统计。
4. 家庭商户的成员邀请方式是邮箱邀请、邀请码，还是两者兼有？建议 MVP 先使用邀请码或邮箱邀请中的一种。
5. 账号方式是先用邮箱/密码，还是第一版就接入国内手机号短信登录？建议先用前者验证产品流程。

## 13. 后续版本候选项

按验证价值排序：

1. 商品快捷键、商品级销售记录和简单库存预警。
2. 账目操作审计、成员操作记录和复杂审批。
3. 账目附件、收据分享与热敏打印。
4. 周报提醒、异常波动提示和经营建议。
5. 与支付渠道或电子表格的数据导入连接。

在开始任一候选项前，先由至少 3 位真实试用商家确认需求频率和业务价值。

## 14. 当前实现状态（2026-08-14）

第一版可运行工程已建立在 `web/` 目录，本机 PostgreSQL 数据库及初始迁移已经配置完成。

### 14.1 已完成

- 工程与数据：Next.js、React、TypeScript、PostgreSQL、Prisma、PWA Manifest、Service Worker 和 IndexedDB 离线队列已接通。
- 账户与权限：账号密码注册/登录/退出、默认家庭商户与摊位、一次性邀请码、成员加入及摊位级服务端权限校验已完成。
- 核心业务：快速新增收入/支出、日结、单摊位/已授权摊位汇总、CSV 导出及离线账目暂存已完成。
- 经营看板：收入、支出、净收益、交易笔数、收款方式和最近账目均可查看。
- 历史分析：支持今天、近 7 天、近 30 天、本月、上月、全部历史及自定义日期；跨度会自动按日、周、月聚合，并记住上次选择。
- 趋势展示：已改为紧凑的收入/支出双折线图，长日期范围自动抽样横轴刻度，并支持悬停查看金额。
- 账目管理：已新增独立 `/records` 页面，支持全部已授权摊位或单摊位、日期、收支类型和收付款方式筛选，以及每页 50 条的游标分页。
- 账目修正：记账员及以上角色可在线编辑账目；管理员和所有者可在二次确认后软删除，操作成功后列表与经营统计会重新读取最新数据。
- 现金日结：选择具体摊位后可查看今日现金收入、现金支出和应有现金；输入收摊实点现金后即时显示相符、多出或短少金额，并可保存复查。
- 成员与摊位管理：已新增独立 `/management` 页面；所有者可查看成员角色与授权范围、调整非所有者成员的角色和摊位、二次确认移除成员，以及新增和修改摊位名称。
- 权限边界：非所有者只能查看自己的成员信息和已授权摊位；新增摊位不会自动授权给既有非所有者成员，历史账目不会因成员移除而删除。
- 界面体验：主要用户界面已统一为简体中文，设置面板已提供账户信息、同步状态和退出登录。
- 开发稳定性：修复日期范围导致的水合不一致，并在开发模式自动注销旧 Service Worker，避免旧缓存干扰热更新。
- 质量验证：当前 TypeScript、ESLint、18 项 Vitest 测试、4 项串行 Playwright 流程和 Next.js 生产构建均通过；邮箱、手机号、账目、摊位及跨账号角色权限已完成真实浏览器验收。

### 14.2 接下来要完成

按优先级推进：

1. 完成货币、时区和常用分类等通用设置；摊位名称已在本阶段完成。
2. 扩展 Playwright 多角色场景，覆盖成员角色调整、授权变更、移除成员、日结、离线恢复和跨账号权限隔离。
3. 完成手机窄屏和桌面端的系统化视觉回归，重点检查空数据、长日期、长邮箱、长文本及异常网络状态。
4. 建立预发布环境，配置 HTTPS、正式数据库、备份恢复、错误监控和强随机会话密钥。
5. 邀请真实商家连续试用，根据反馈决定是否进入商品快捷录入、库存提醒等后续版本。

### 14.3 本轮复盘

**阶段结论：账目管理闭环与现金日结已完成。**

- 账目闭环：用户可从首页进入独立账目页，在全部已授权摊位或单摊位范围内按日期、类型和收付款方式查找账目，并按游标继续加载历史记录。
- 修正能力：记账员及以上角色可编辑，管理员和所有者可二次确认后软删除；修改后账目列表和经营统计会同步刷新，越权摊位仍由服务端拦截。
- 经营展示：历史范围支持按日、周、月聚合；原柱状图在窄屏下过高且溢出，现已改为紧凑的收入/支出双折线图。
- 现金日结：日结已从单纯保存数字变为可见的现金核对流程，明确展示现金收入、现金支出、应有现金及实点差额，并可恢复当天已保存的数据。
- 移动体验：底部四个主要操作已统一为等宽、等高布局；账目闭环在 375px 手机和 1280px 桌面视口完成截图及横向溢出检查，日结和导航另在 390px 手机视口验证。
- 质量结果：TypeScript、ESLint、10 项 Vitest 测试和生产构建通过；隔离测试数据已验证账目新增、筛选、编辑、软删除和日结现金差额。
- 过程改进：涉及图表、底部导航或响应式表单的改动，今后除静态检查和构建外，必须完成真实手机视口截图与溢出检查后交付。

### 14.4 成员与摊位管理阶段原定范围

**目标：**让所有者无需操作数据库即可维护家庭商户成员和摊位授权，形成权限管理闭环。

1. 建立成员管理界面，展示成员姓名或账号、角色、状态及当前获授权摊位。
2. 仅允许所有者调整成员角色和摊位授权；管理员、记账员和查看者只能查看与自身相关的信息，不能扩大权限。
3. 支持所有者移除成员，操作前二次确认；移除只撤销后续访问，不删除该成员留下的历史账目。
4. 建立摊位管理界面，支持新增摊位、修改摊位名称，并清楚展示每个摊位的成员授权情况。
5. 新增摊位默认不自动授权给既有非所有者成员，必须由所有者明确分配。
6. 补充接口校验、角色矩阵测试和 Playwright 流程，覆盖越权修改拒绝、移除成员、创建摊位及手机窄屏布局。
7. 完成后再次运行 TypeScript、ESLint、Vitest、Playwright 和生产构建，并进行手机与桌面截图验证。

本阶段不包含货币、时区、常用分类等通用设置，也不包含预发布部署、短信登录、复杂审批或成员操作审计；这些继续保留在后续阶段，避免同时扩大权限与配置变更范围。

### 14.5 成员与摊位管理阶段完成记录

- 已完成成员与摊位独立页面及首页设置入口，保持首页日常记账操作简洁。
- 所有者可修改管理员、记账员和查看者角色，并为其重新分配一个或多个摊位；服务端禁止转让所有者、空授权、重复授权和跨家庭商户摊位授权。
- 移除成员前会明确提示影响；数据库仅删除成员关系和授权，不删除其创建或修改过的历史账目。
- 所有者可新增摊位和修改名称；新摊位默认只对所有者可见，需手动加入其他成员的授权范围。
- 非所有者访问管理页时为只读状态，接口只返回本人信息和已授权摊位，避免借管理页面扩大数据可见范围。
- 验证结果：13 项 Vitest、完整 ESLint、TypeScript、Next.js 生产构建和 2 项 Playwright 流程通过；管理页已覆盖 Pixel 5 手机尺寸和 1280px 桌面宽度的横向溢出检查。
- 后续重点转为通用设置、多角色端到端权限回归、预发布环境与真实商家试用。

### 14.6 本阶段复盘与下一阶段安排

**阶段结论：成员与摊位管理闭环已完成。**

- 完成情况：原定的成员查看、角色调整、摊位授权、成员移除、摊位新增与改名均已实现；管理能力集中在独立页面，没有增加首页日常记账负担。
- 权限结果：所有管理写操作均要求所有者权限；非所有者只读取本人和已授权摊位，前端隐藏操作的同时服务端继续独立校验。
- 数据结果：成员移除不会破坏历史账目；新增摊位不会意外继承旧成员授权，符合最小权限原则。
- 测试结果：13 项 Vitest、2 项串行 Playwright、TypeScript、ESLint 和生产构建通过；手机与桌面宽度均无横向溢出。
- 已关闭风险：自动化流程已创建第二个真实测试账号，完成管理员、记账员和查看者三种身份、邀请注册、角色切换、授权撤销和移除后会话失效的跨账号回归。
- 过程改进：使用同一本地数据库且受注册限流保护的 Playwright 流程应串行运行，避免并发注册造成与业务无关的假失败。

**下一阶段：通用设置与质量加固。**

1. 建立设置页面，支持家庭商户货币、时区和常用收支分类的查看与修改，并明确哪些变更会影响历史数据展示。
2. 分类设置支持新增、改名、排序和停用；历史账目保留原分类文本，不因分类停用而丢失或被批量修改。
3. 覆盖日结保存与恢复、离线记账恢复同步、长文本、空数据和网络失败状态。
4. 完成后运行 TypeScript、ESLint、Vitest、Playwright 和生产构建，并进行手机与桌面视觉验收。

下一阶段不包含预发布部署、短信登录、商品库存、复杂审批和审计日志；通用设置与权限回归稳定后，再单独进入发布准备阶段。

### 14.7 成员权限缺陷补齐记录

- 受邀注册已改为单事务：有效邀请码会直接创建邀请方成员关系和指定摊位授权，不再额外创建空家庭商户；邀请码无效、过期、已使用或摊位映射失效时不创建账号。
- 已有账号仍可在登录时接受邀请码；当前产品继续采用单一有效家庭商户，不引入商户切换。
- 成员被移除后，旧 JWT 的后续业务请求会返回 `MEMBER_REMOVED`；刷新页面会清除会话并回到登录页，明确提示已被移出家庭商户。
- 权限错误已区分非成员、未授权摊位、无写入权限、无删除权限和仅所有者操作，便于前端提示和自动化断言。
- 跨账号 Playwright 已实际验证：管理员可编辑和删除授权摊位账目但不能管理成员或摊位；记账员可新增、编辑和日结但不能删除；查看者只能浏览，写入和日结均被服务端拒绝。
- 授权变更无需更换 JWT 即时生效；撤销摊位后旧页面无法继续查询该摊位，移除成员后历史账目仍保留在原家庭商户。
- 所有者不能修改或移除自己，其他成员不能被提升为所有者；未授权摊位无法通过篡改请求参数访问。
- Playwright 固定单 worker；开发和自动化环境使用更高的注册限流阈值，生产环境仍保持每分钟 5 次注册保护。
- 最终验证：15 项 Vitest、3 项 Playwright、完整 ESLint、TypeScript 和生产构建通过；成员移除提示在 Pixel 5 和 1280px 桌面宽度下无横向溢出。

该缺口现已关闭，可以恢复 14.6 中的“通用设置与质量加固”阶段。

## 15. 手机号/邮箱认证与密码找回阶段（2026-08-14）

### 15.1 已完成

- 账号模型：邮箱改为可空，新增唯一手机号与会话版本；数据库约束保证每个用户至少拥有邮箱或手机号之一，现有邮箱账号原样保留。
- 手机规范：首版仅支持中国大陆手机号，服务端统一保存为 `+86` E.164，成员列表只显示脱敏手机号。
- 统一登录：登录框支持手机号或邮箱自动识别，旧 `{ email, password }` 接口请求仍兼容；失败提示不暴露账号是否存在。
- 注册流程：保留邮箱密码注册，新增手机号、短信验证码和密码注册；两种方式都支持邀请码，受邀注册直接加入邀请方。
- 密码找回：手机号账号可通过短信验证码设置新密码；重置后递增会话版本，旧 JWT 和旧密码立即失效。
- 验证码安全：6 位验证码、5 分钟有效、最多 5 次尝试、60 秒发送间隔、手机号日限额与 IP 小时限额；数据库只保存加盐哈希。
- 短信架构：已实现腾讯云短信和 mock 适配器；生产环境默认禁止 mock，腾讯云密钥、应用、签名和模板均通过服务端环境变量配置。
- 界面：注册页提供手机号/邮箱分段选择，登录页统一账号框，新增验证码倒计时和手机号找回密码流程。
- 数据迁移：`20260814090000_phone_auth` 已在本机 PostgreSQL 成功应用，未修改现有用户 ID、密码哈希、成员关系、邀请或账目。
- 验证结果：Prisma schema 与迁移状态、TypeScript、ESLint、18 项 Vitest、4 项串行 Playwright 和生产构建通过；手机号注册登录重置、旧会话失效、手机号邀请码及邮箱兼容均已验证。

### 15.2 安全与运维边界

- 纯邮箱账号暂不支持自助找回密码；本阶段不做邮箱验证码、手机号绑定、账号合并或国际手机号。
- 同一自然人分别用邮箱和手机号注册会形成两个独立账号，不自动合并经营数据。
- 模拟验证码仅用于开发与自动化测试，生产环境需要显式腾讯云配置；测试专用生产式启动开关不得配置在正式部署环境。
- 正式短信尚未开通：需准备腾讯云实名认证、短信应用、审核通过的签名和注册/重置模板，并配置余额告警和发送失败监控。

### 15.3 后续安排

1. 获取腾讯云短信资质与模板后，在预发布环境发送真实短信，验证模板参数、签名、频率限制和失败告警。
2. 恢复“通用设置与质量加固”阶段，继续完成货币、时区、常用分类、日结恢复、离线同步和异常状态回归。
3. 真实用户出现邮箱找回或账号绑定需求后，再单独设计邮件服务、绑定验证与人工申诉流程。

### 15.4 阶段复盘与关闭结论

**阶段状态：DONE。手机号/邮箱认证与手机号密码找回的代码、迁移、界面和自动化验收均已完成。**

- 交付结果：现有邮箱账号继续兼容；新用户可用邮箱加密码注册，也可用中国大陆手机号、短信验证码和密码注册；登录统一接受手机号或邮箱；手机号账号可通过短信验证码重置密码。
- 邀请闭环：邮箱和手机号注册都能携带家庭邀请码，注册成功后直接加入邀请方家庭商户，不产生额外空商户。
- 会话安全：密码重置会递增 `sessionVersion`，重置前签发的 JWT 在下一次请求时失效；验证码仅保存加盐哈希，并受有效期、尝试次数、发送间隔、手机号日限额和 IP 限额保护。
- 供应商边界：业务层已通过 `SmsProvider` 隔离短信供应商，本地和自动化测试使用固定验证码 `246810` 的 mock 实现，生产环境默认禁止 mock；腾讯云实现已接入官方 SDK。
- 数据兼容：迁移 `20260814090000_phone_auth` 已应用到本机数据库，保留了原用户 ID、密码哈希、成员关系、邀请和账目数据。
- 验收结果：TypeScript、ESLint、Prisma schema 与迁移状态、18 项 Vitest、4 项串行 Playwright 和 Next.js 生产构建全部通过；覆盖邮箱兼容、手机号注册/登录、邀请注册、密码重置、旧会话失效及跨账号权限流程。
- 当前限制：真实腾讯云短信尚未完成线上资质和模板验收，因此当前只能把手机号认证标记为“代码完成、mock 环境可用”，不能标记为“生产短信已启用”。

**接下来要完成：**

1. 恢复“通用设置与质量加固”阶段，完成货币、时区和常用收支分类设置，并补充日结恢复、离线同步、空数据和异常网络状态回归。
2. 建立预发布环境，使用独立数据库、HTTPS、强随机会话密钥、备份恢复和错误监控完成发布演练。
3. 准备腾讯云实名认证、短信应用、签名、注册模板和重置密码模板；在预发布环境验证真实短信参数、频率限制、余额告警和发送失败监控后，才启用 `SMS_PROVIDER=tencent`。
4. 根据真实用户反馈再决定是否立项邮箱找回密码、邮箱验证码、已有账号绑定手机号、账号合并和国际手机号；这些能力当前均不自动开启。

## 16. 通用设置与质量加固阶段（2026-08-14）

### 16.1 已完成

- 新增独立 `/settings` 页面，所有成员可查看家庭商户货币、时区和常用分类，仅所有者可修改。
- 货币固定为人民币 `CNY`；家庭时区支持北京时间与乌鲁木齐时间，并同步到全部摊位。家庭商户产生账目或日结后时区自动锁定，避免历史营业日错位。
- 新增家庭共享的收入/支出常用分类，支持新增、改名、排序、启用和停用；收入与支出至少各保留一个启用分类。
- 新商户自动建立默认分类；迁移为既有商户回填系统默认分类和历史高频分类，不改写历史账目分类文本。
- 记账弹窗可优先选择启用的常用分类，也可直接输入临时分类；离线队列继续保存分类文本。
- 账目日期筛选、经营统计和日结营业日边界已接入家庭时区；金额格式化支持从家庭商户传入货币，当前只使用人民币。
- 离线待同步操作新增用户与家庭商户归属，统计和同步只处理当前账号，避免切换账号后误同步。同步接口继续依靠 `clientOperationId` 保证幂等。
- 数据迁移 `20260814153000_general_settings` 已应用，本机数据库迁移状态为最新，既有账目、日结、成员和邀请均未删除或重写。
- 验收结果：TypeScript、ESLint、Prisma 校验、20 项 Vitest、5 项串行 Playwright 和 Next.js 生产构建全部通过。设置页已在 Pixel 5 与 1280px 桌面宽度检查无横向溢出。

### 16.2 后续安排

1. 进入“预发布与正式发布准备”阶段，建立独立预发布数据库、HTTPS、备份恢复、错误监控和强随机生产密钥。
2. 在预发布环境完成真实腾讯云短信签名与模板验收、余额告警和发送失败监控。
3. 邀请少量真实商户连续试用，重点观察分类维护、跨时区营业日、离线恢复、日结和导出体验。
4. 根据试用反馈再决定邮箱找回、账号绑定、商品库存、审计日志和多币种账本，不在当前版本提前扩展。

### 16.3 阶段复盘与关闭结论

**阶段状态：DONE。通用设置、常用分类、家庭时区和离线队列隔离已经形成可用闭环。**

- 产品闭环：用户可以从首页进入独立设置页；所有者维护家庭时区和常用分类，其他成员只能查看，服务端不依赖前端隐藏按钮执行权限控制。
- 数据策略：分类目录与历史账目文本分离，新增、改名或停用分类不会批量改写历史数据；既有商户通过迁移获得默认及历史高频分类，新商户在注册事务中获得默认分类。
- 日期策略：家庭商户统一使用北京时间或乌鲁木齐时间；首笔账目或日结产生后锁定时区，账目筛选、经营统计和日结按该营业日边界计算。
- 离线策略：待同步操作绑定用户和家庭商户，只同步当前会话所属队列；服务端继续使用操作 ID 防止重复入账。
- 权限边界：货币本阶段固定为人民币；只有所有者可修改设置和分类，且收入、支出至少各保留一个启用分类。
- 验收结果：数据库迁移已应用；TypeScript、ESLint、Prisma 校验、20 项 Vitest、5 项串行 Playwright 和生产构建通过；设置页在手机与桌面宽度下无横向溢出。
- 已知边界：尚未建立预发布环境，真实腾讯云短信、备份恢复和线上监控尚未验收；多币种、账号绑定、邮箱找回、商品库存和审计日志仍不在当前版本范围。

**下一阶段起点：预发布与正式发布准备。** 先完成环境架构、密钥与数据库隔离、部署和回滚方案，再执行任何真实部署；不得把本机 `.env`、mock 短信开关或本地测试数据带入预发布及生产环境。
## 17. 小店经营本视觉升级阶段（2026-08-14）

**阶段状态：DONE。** 本阶段只调整视觉表达和交互呈现，未改动账目、权限、认证、离线队列、数据库或 API 合同。

- 建立暖米白纸张、墨绿、杏黄、砖红与深炭灰的全局视觉令牌；组件统一使用轻边框、8px 圆角和克制阴影。
- 新增“账本 + 小店”品牌标识 `web/public/shop-ledger-mark.svg`，同时用于页面元数据与 PWA 图标。
- 首页改为紧凑的店铺抬头、账本式收益层级与暖色双折线趋势；收入、支出和风险状态均保留文字说明。
- 底部导航改为四个等宽、固定高度的图标加标签项：记一笔、账目、日结、导出，窄屏不再因文本长度变形。
- 账目、设置、成员管理、底部表单、空状态和反馈提示统一为小店经营本样式；长文本保持换行或截断，避免横向滚动。
- 使用 Lucide 图标补齐主要操作的识别与焦点表达，并保留关键控件的可访问名称。

验收结果：TypeScript、ESLint、20 项 Vitest、5 项串行 Playwright 和生产构建全部通过。Playwright 已覆盖管理、跨账号权限、手机号认证、账目编辑删除与设置流程；手机与桌面断言持续检查横向溢出。

下一阶段：在不改变当前界面与本地数据的前提下，进入预发布与正式发布准备，包括独立环境、HTTPS、备份恢复、监控、真实腾讯云短信验收与少量真实商户试用。
