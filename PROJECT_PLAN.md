# 摊主日记账：开发计划与架构规范

> 文档状态：`v0.2`
> 最后更新：2026-08-14  
> 适用范围：本仓库内的产品、开发、测试、部署及多 Agent 协作。  
> 本文档是项目当前的单一事实来源；范围或架构变更时，先更新本文档，再开始实现。

## 1. 产品定位

为线下摆摊、流动经营和微型商户提供一款**手机优先的日常收银记录工具**。商家可在几秒内记录收入和支出，并随时查看今天、本周、本月的经营数据。

核心原则：

- **快**：单笔收支录入不超过 3 个主要操作。
- **稳**：网络不稳定时仍可录入；恢复网络后自动同步。
- **清楚**：首页优先呈现收入、支出、净收益和收款构成。
- **安全**：每位商家只能访问自己的数据；金额、账目和备份可追溯。
- **克制**：第一版先解决日记账与经营看板，不做完整进销存或支付系统。

## 2. 目标用户与关键场景

| 用户 | 场景 | 需要完成的动作 |
| --- | --- | --- |
| 单人摊主 | 营业中快速补记一笔收款 | 选择收入、收款方式、金额后保存 |
| 单人摊主 | 收摊时核对现金 | 查看现金应有金额，录入实点金额并确认差额 |
| 小店老板 | 想知道近期经营是否变好 | 查看日/周/月趋势、支付方式占比与净收益 |
| 家庭商户所有者 | 全家经营多个摊位 | 查看全部或单个摊位的数据，邀请家人并分配权限 |
| 家庭成员 / 合伙人 | 负责一个或多个摊位 | 仅查看或记录被授权摊位的账目 |

## 3. MVP 范围（第一版必须完成）

### 3.1 功能清单

1. **账户、家庭商户与摊位**
   - 注册、登录、退出；初始版本使用账号密码登录，手机号短信验证作为后续增强。
   - 新用户创建一个默认家庭商户和默认摊位；摊位数据归家庭商户，不归某个个人账号。
   - 所有者可邀请家人或合伙人加入，并为其授权全部摊位或指定摊位；第一版仅所有者可管理成员，防止成员自行扩大权限。
   - 支持四种角色：所有者、管理员、记账员、查看者；具体权限见第 5 节。
2. **今日记账**
   - 新增收入或支出：金额、发生时间、收款/付款方式、分类、备注。
   - 支持现金、微信、支付宝、银行卡、其他五种方式。
   - 可编辑、删除、按日期筛选账目。
3. **日结与对账**
   - 记录开档备用金、实点现金、收摊备注。
   - 自动计算应有现金：`开档备用金 + 现金收入 - 现金支出`。
   - 显示实点差额，但不阻止商家完成日结。
4. **经营看板**
   - 今日收入、今日支出、今日净收益、今日交易笔数。
   - 最近 7 天收入/净收益趋势图。
   - 本月收入、支出、净收益汇总。
   - 今日收款方式占比与最近账目列表。
   - 有多个摊位时，可切换“全部摊位”与任一被授权摊位；全部摊位数据为已授权摊位的聚合。
5. **移动端与数据可靠性**
   - 响应式网页，优先适配 360–430px 宽的手机。
   - 支持安装为 PWA；已访问页面和待同步账目可离线使用。
   - 离线操作先保存在本机队列，联网后自动同步；用户能看到同步状态。
6. **数据导出与基础设置**
   - 按时间范围导出 CSV。
   - 设置默认货币、时区、摊位名称和常用收支分类。

### 3.2 明确不在 MVP 中

- 对接微信支付、支付宝或银行卡的真实资金流水。
- 库存、采购、供应商、会员、营销、外卖、电子发票和打印机。
- 复杂审批、完整会计报表、AI 经营建议。
- 多商家 SaaS 计费与订阅。

任何新增功能必须先判断是否直接提高“快速记账”或“看懂经营数据”的能力；否则放入后续版本。

## 4. 信息架构与页面

| 路由 | 页面目标 | 主要内容 |
| --- | --- | --- |
| `/login` | 进入系统 | 登录、注册、找回密码入口 |
| `/onboarding` | 首次设置 | 家庭商户名、默认摊位名、货币、常用分类 |
| `/today` | 营业中的主页面 | 当前摊位选择、今日指标、快速记一笔、最近账目、日结入口 |
| `/records` | 查账与修正 | 摊位范围与日期筛选、账目列表、新增/编辑/删除 |
| `/analytics` | 经营数据展示 | 单摊位/全部摊位切换、7 天趋势、月度汇总、收款方式占比 |
| `/settings` | 管理数据与偏好 | 家庭商户、摊位、成员权限、分类、导出、账户与同步状态 |

手机端固定底部导航为：`今日`、`记一笔`、`账目`、`数据`、`我的`。其中“记一笔”是最醒目的主操作。

## 5. 数据与业务规则

### 5.1 核心实体

| 实体 | 关键字段 | 说明 |
| --- | --- | --- |
| `users` | `id`、`email`、`password_hash`、`created_at` | 登录主体；可在后续增加手机号 |
| `organizations` | `id`、`name`、`currency`、`timezone`、`created_at` | 家庭商户 / 合伙商户，是摊位与经营数据的归属主体 |
| `organization_members` | `organization_id`、`user_id`、`role`、`status`、`joined_at` | 家庭成员或合伙人；角色为 `owner`、`admin`、`bookkeeper`、`viewer` |
| `stores` | `id`、`organization_id`、`name`、`timezone` | 摊位/门店，归属于家庭商户；不再直接归属于用户 |
| `member_store_permissions` | `member_id`、`store_id`、`access_level` | 指定摊位授权；所有者默认拥有全部摊位权限，其他角色必须有明确授权 |
| `ledger_entries` | `id`、`store_id`、`type`、`amount_fen`、`payment_method`、`category`、`occurred_at`、`note` | 一笔收入或支出的事实来源 |
| `daily_closings` | `id`、`store_id`、`business_date`、`opening_cash_fen`、`actual_closing_cash_fen`、`status` | 每日营业与现金对账状态 |
| `sync_operations` | `id`、`store_id`、`client_operation_id`、`status`、`processed_at` | 离线同步的幂等记录 |
| `audit_logs`（后续） | `actor_id`、`action`、`target_id`、`created_at` | 关键数据变更追踪 |

### 5.2 不可违反的规则

- 金额统一使用整数分 `amount_fen` 保存和计算，展示时再格式化为元；禁止用浮点数处理金额。
- `ledger_entries.type` 只能是 `income` 或 `expense`；金额必须大于 0。
- 摊位数据属于 `organizations`，不属于个人账号；成员离开后历史账目必须保留在该家庭商户名下。
- 所有查询和写入都必须校验：当前用户是该 `organization` 的有效成员，且拥有目标 `store` 所需的访问级别；不能只依赖前端隐藏数据。
- `owner` 可管理全部摊位、成员、设置和导出；`admin` 可管理获授权摊位及其账目，但不能管理成员；`bookkeeper` 可查看并记录获授权摊位账目；`viewer` 仅可查看获授权数据。
- 邀请仅在接受后生效；移除成员只撤销后续访问，不删除其历史操作和账目。
- “全部摊位”汇总只聚合当前用户有权访问的摊位，不能因汇总接口泄露未授权摊位数据。

### 5.3 角色与摊位范围矩阵

| 角色 | 默认可访问范围 | 允许的操作 | 不允许的操作 |
| --- | --- | --- | --- |
| `owner` | 该家庭商户的全部摊位 | 管理摊位、账目、日结、导出、成员邀请与移除 | 无法访问其他家庭商户 |
| `admin` | 仅被明确授权的摊位 | 管理获授权摊位的账目、日结和基础设置 | 邀请/移除成员、修改角色、访问未授权摊位 |
| `bookkeeper` | 仅被明确授权的摊位 | 新增、查看和编辑本人获授权摊位的账目与日结 | 删除已同步账目、导出、管理成员或摊位设置 |
| `viewer` | 仅被明确授权的摊位 | 查看账目和数据看板 | 新增、编辑、删除、导出、日结或设置 |

“授权全部摊位”在实现上应为该成员对当前所有摊位拥有授权，并在新增摊位时由所有者选择是否自动继承；MVP 默认**不自动继承**，避免新开摊位的数据被意外共享。
- 同一摊位、同一营业日期最多只有一条 `daily_closings` 记录。
- 收入、支出和净收益由账目聚合得出；不得把看板汇总值当作唯一数据来源。
- 删除采用用户可见的软删除或审计记录；不能静默抹去已同步账目。
- 所有“今天”与“营业日”均按该摊位设置的时区计算，默认 `Asia/Shanghai`。

## 6. 技术架构

第一版采用**模块化单体应用**：部署简单、团队协作边界清晰，后续可按模块拆分，不提前引入微服务复杂度。

```text
手机浏览器 / PWA
  ├─ Next.js + React + TypeScript 用户界面
  ├─ IndexedDB：离线账目与同步队列
  └─ Service Worker：静态资源和最近页面缓存
            │ HTTPS / JSON API
            ▼
Next.js 服务端（Route Handlers）
  ├─ 身份认证、组织成员与摊位访问控制
  ├─ 记账、日结、数据分析 API
  ├─ 输入校验与同步幂等处理
  └─ Prisma 数据访问层
            │
            ▼
PostgreSQL
  ├─ 用户、门店、账目、日结
  └─ 定时备份与恢复演练
```

### 6.1 推荐技术栈

| 层级 | 选择 | 原因 |
| --- | --- | --- |
| 应用框架 | Next.js + React + TypeScript | 同一项目完成前端、API 与服务端渲染，开发成本低 |
| UI | Tailwind CSS + 统一组件库 | 快速实现清晰的大触控界面 |
| 数据库 | PostgreSQL | 适合可靠事务、聚合分析与后续多门店扩展 |
| ORM 与迁移 | Prisma | 数据模型、迁移和类型检查集中管理 |
| 表单校验 | Zod | 客户端与服务端共享校验规则 |
| 图表 | Recharts | 适合趋势图和占比图，移动端易定制 |
| 离线存储 | IndexedDB（建议 Dexie 封装） | 容量和查询能力优于 localStorage |
| PWA | Service Worker + Web App Manifest | 支持添加到手机桌面和弱网可用 |
| 测试 | Vitest、React Testing Library、Playwright | 覆盖逻辑、界面和关键用户流程 |

### 6.2 离线与同步策略

1. 用户新增或修改账目时，界面立即显示结果，同时写入 IndexedDB 的本地账目和待同步队列。
2. 每条同步操作由客户端生成全局唯一的 `client_operation_id`。
3. 网络恢复、应用重新打开或用户主动点击同步时，按时间顺序提交队列。
4. 服务端以 `client_operation_id` 做幂等校验；重复请求必须返回原结果，不能重复记账。
5. 同步成功后更新本地状态；失败时保留队列并显示可理解的错误原因。
6. 首页缓存最近一次成功同步的看板数据，但明确标识“数据更新时间”。

MVP 不处理多设备同时编辑同一笔账目的复杂冲突；第一版采用“最后一次成功提交的修改生效”，并保留更新时间。若真实用户需要，第二版再引入版本号和冲突提示。

## 7. 接口边界（MVP）

| 方法与路径 | 职责 |
| --- | --- |
| `POST /api/auth/register` | 创建账户、默认家庭商户与默认摊位 |
| `POST /api/auth/login` | 建立登录会话 |
| `GET /api/organizations/current` | 获取当前家庭商户、成员角色与可访问摊位 |
| `GET /api/stores` | 获取当前成员可访问的摊位列表 |
| `POST /api/organizations/current/invitations` | 邀请成员，并设定角色与摊位范围（仅所有者） |
| `PATCH /api/organization-members/:id` | 调整成员角色、摊位授权或移除成员（仅所有者） |
| `GET /api/ledger-entries` | 按日期、类型、分类查询账目 |
| `POST /api/ledger-entries` | 新建收入或支出，支持同步操作 ID |
| `PATCH /api/ledger-entries/:id` | 修改账目 |
| `DELETE /api/ledger-entries/:id` | 软删除账目 |
| `GET /api/daily-closing/:date` | 获取某日营业和现金对账 |
| `PUT /api/daily-closing/:date` | 保存或更新日结 |
| `GET /api/analytics/summary` | 返回当前成员有权访问的指定摊位或全部摊位的汇总、趋势与占比 |
| `GET /api/exports/ledger.csv` | 导出当前成员有权访问范围内的账目 CSV |

所有写入接口均需要：认证、门店归属校验、字段校验、金额范围校验、结构化错误码和请求日志。

## 8. 代码结构约定

```text
src/
  app/                 # 页面与 API 路由
  components/          # 跨模块通用 UI 组件
  features/
    auth/              # 登录、会话、权限
    ledger/            # 账目领域
    closing/           # 日结与现金对账
    analytics/         # 看板、聚合、图表
    organizations/     # 家庭商户、成员邀请、角色与摊位授权
    stores/            # 摊位与分类设置
    sync/              # IndexedDB、离线队列、同步状态
  lib/                 # 数据库、校验、日期、金额等基础工具
  styles/
prisma/                # 数据模型与迁移
public/                # 图标、manifest、静态资源
tests/
  unit/
  integration/
  e2e/
```

约定：领域逻辑放在 `features/<领域>`，不能把业务计算散落在页面组件中；金额、日期、权限和接口校验使用共享工具，禁止重复实现。

## 9. 分阶段任务与验收标准

| 阶段 | 任务 | 完成标准 | 依赖 |
| --- | --- | --- | --- |
| P0：产品基线 | 确认字段、页面原型、本文档 | MVP 范围、术语和验收标准均已确认 | 无 |
| P1：工程基座 | 初始化项目、代码规范、环境变量、组织/成员/摊位迁移、认证与角色授权骨架 | 新成员可按 README 启动，角色矩阵对应的权限测试可运行 | P0 |
| P2：每日记账 | 账目 CRUD、分类、金额与日期校验、今日列表 | 手机端可完整新增、编辑、删除一笔账目 | P1 |
| P3：日结与看板 | 今日汇总、现金对账、单/全部摊位切换、7 天趋势、月度汇总 | 聚合数值与测试数据一致，空数据状态友好 | P2 |
| P4：移动与离线 | PWA、IndexedDB 队列、同步、同步状态 | 断网新增账目后恢复网络，服务端仅产生一笔记录 | P2 |
| P5：质量与安全 | 自动化测试、成员权限测试、错误处理、导出与备份 | 关键 E2E 流程通过，不存在跨组织或越权摊位访问 | P2–P4 |
| P6：发布试用 | 预发布环境、域名、HTTPS、监控、试用反馈 | 手机可安装访问，试用商家可连续日常记账 | P5 |

### P2/P3 必测业务样例

- 收入 25.50 元、支出 4.80 元时，净收益显示 20.70 元。
- 开档现金 100 元、现金收入 220 元、现金支出 30 元时，应有现金为 290 元。
- 删除一笔收入后，今日、周趋势和月度汇总应同时更新。
- 空账目、无网络、同步失败、无权限访问和重复同步请求均有明确界面或错误响应。
- 所有者可看两个摊位的合计与单摊位数据；记账员无法查询、导出或修改未被授权摊位的数据。

## 10. 多 Agent 协作规范

### 10.1 任务状态与交接

状态只能按以下顺序流转：`TODO → IN_PROGRESS → REVIEW → DONE`；遇到外部依赖或无法判断的产品决策时标记为 `BLOCKED` 并写明原因。

每个任务开始前必须记录以下信息（在 Issue、任务卡或协作消息中）：

```text
任务 ID：P2-LEDGER-01
目标：实现账目新增与金额校验
范围：src/features/ledger、src/app/api/ledger-entries
不包含：日结、图表、离线同步
依赖：P1-DB-01
验收：新增一笔收入/支出后列表和今日汇总正确；测试通过
验证命令：<由实现 Agent 补充>
```

交接时必须说明：已改文件、验证结果、未解决问题、后续 Agent 的首个动作。没有验证结果的任务不得标记 `DONE`。

### 10.2 文件与边界

- 同一时间同一文件只能由一个 Agent 主导修改；需要协作时先约定拆分点。
- Agent 不得顺手重构无关模块，也不得修改未声明的环境、部署或权限配置。
- 数据库 schema、迁移、认证、组织权限和共享类型属于高冲突区域：修改前必须声明，修改后要求至少一名其他 Agent 审查。
- 新增依赖必须说明用途、体积/维护风险和替代方案；未经确认不得引入付费 SaaS、真实支付 SDK 或收集额外个人信息。
- 不执行批量或递归删除；删除单个明确文件前先确认它不再被引用。

### 10.3 代码与测试规范

- TypeScript 开启严格模式；对外 API 参数和返回值必须有类型与 Zod 校验。
- 业务金额只能用“分”的整数和专用金额工具处理。
- 新功能至少包含相应的单元测试；影响关键流程时补充 E2E 测试。
- 所有用户可见文案使用简体中文；错误信息应告诉用户下一步可做什么。
- 提交实现前依次运行：类型检查、相关单元测试、格式检查；影响登录/记账/同步时运行对应 E2E。
- 禁止在代码、文档、测试或截图中写入密钥、真实手机号、真实账目或生产数据库连接串。

### 10.4 审查清单

- 是否只实现了任务范围内的内容？
- 任意金额计算是否避免了浮点误差？
- 任意接口是否完成认证、组织成员资格与摊位授权校验？
- 修改/删除账目后，所有相关统计是否一致？
- 手机窄屏、加载中、空数据、失败和离线状态是否可用？
- 是否有测试或可重复验证步骤？

## 11. 安全、隐私与运维

- 使用 HTTPS；会话 Cookie 设置 `HttpOnly`、`Secure`、`SameSite`。
- 密码只能存安全哈希；登录接口应限流，服务端密钥仅放在环境变量中。
- 数据库按最小权限连接；生产环境开启自动备份，并定期做恢复演练。
- 账目属于经营敏感数据，导出文件由用户主动生成；日志不得记录完整账目备注、密码或令牌。
- 首次正式上线前准备隐私政策、数据删除流程和用户反馈渠道。
- 若生产服务器部署在中国大陆，需在上线计划中安排域名与 ICP 备案；若提供收费在线服务、真实支付或更广泛的数据处理，发布前进行专项合规评估。

## 12. 上线策略与待决策项

### 12.1 环境

| 环境 | 用途 | 数据规则 |
| --- | --- | --- |
| 本地开发 | 功能开发与自动测试 | 仅使用假数据和本地数据库 |
| 预发布 | 集成验证与邀请试用 | 与生产隔离，可重置测试数据 |
| 生产 | 真实商家使用 | 备份、监控、最小权限、严格环境变量 |

### 12.2 域名与发布

- 开发和小范围试用可先使用托管平台提供的 HTTPS 测试域名。
- 对外正式运营前注册独立域名，并绑定生产环境；同时设置监控、错误告警、备份与数据导出。
- PWA 需要 HTTPS 才能稳定提供安装和离线能力；独立域名不是开发前置条件，但应是正式发布前置条件。

### 12.3 需要产品负责人确认的事项

1. 第一批试用商家是否只在中国大陆使用？这决定部署区域、备案节奏和访问优化。
2. MVP 是否仅做“手工录入总额/单笔收支”，还是提供商品级快速点单？建议前者。
3. 是否需要区分微信与支付宝的“实际到账”和“展示金额”？建议 MVP 先按手工录入的实际金额统计。
4. 家庭商户的成员邀请方式是邮箱邀请、邀请码，还是两者兼有？建议 MVP 先使用邀请码或邮箱邀请中的一种。
5. 账号方式是先用邮箱/密码，还是第一版就接入国内手机号短信登录？建议先用前者验证产品流程。

## 13. 后续版本候选项

按验证价值排序：

1. 商品快捷键、商品级销售记录和简单库存预警。
2. 账目操作审计、成员操作记录和复杂审批。
3. 账目附件、收据分享与热敏打印。
4. 周报提醒、异常波动提示和经营建议。
5. 与支付渠道或电子表格的数据导入连接。

在开始任一候选项前，先由至少 3 位真实试用商家确认需求频率和业务价值。

## 14. 当前实现状态（2026-08-14）

第一版可运行工程已建立在 `web/` 目录，本机 PostgreSQL 数据库及初始迁移已经配置完成。

### 14.1 已完成

- 工程与数据：Next.js、React、TypeScript、PostgreSQL、Prisma、PWA Manifest、Service Worker 和 IndexedDB 离线队列已接通。
- 账户与权限：账号密码注册/登录/退出、默认家庭商户与摊位、一次性邀请码、成员加入及摊位级服务端权限校验已完成。
- 核心业务：快速新增收入/支出、日结、单摊位/已授权摊位汇总、CSV 导出及离线账目暂存已完成。
- 经营看板：收入、支出、净收益、交易笔数、收款方式和最近账目均可查看。
- 历史分析：支持今天、近 7 天、近 30 天、本月、上月、全部历史及自定义日期；跨度会自动按日、周、月聚合，并记住上次选择。
- 趋势展示：已改为紧凑的收入/支出双折线图，长日期范围自动抽样横轴刻度，并支持悬停查看金额。
- 界面体验：主要用户界面已统一为简体中文，设置面板已提供账户信息、同步状态和退出登录。
- 开发稳定性：修复日期范围导致的水合不一致，并在开发模式自动注销旧 Service Worker，避免旧缓存干扰热更新。
- 质量验证：当前 ESLint、6 项 Vitest 测试和 Next.js 生产构建均通过。

### 14.2 接下来要完成

按优先级推进：

1. 补齐账目查询、编辑和删除的完整界面，并为修改后统计联动增加测试。
2. 补齐成员管理和摊位管理界面，包括调整角色、授权范围、移除成员及新增摊位。
3. 完成货币、时区、摊位名称和常用分类等设置项；目前设置面板以账户和同步信息为主。
4. 增加 Playwright 端到端测试，覆盖注册登录、记一笔、日期筛选、日结、离线恢复和权限隔离。
5. 完成手机窄屏和桌面端的系统化视觉回归，重点检查空数据、长日期、长文本及异常网络状态。
6. 建立预发布环境，配置 HTTPS、正式数据库、备份恢复、错误监控和强随机会话密钥。
7. 邀请真实商家连续试用，根据反馈决定是否进入商品快捷录入、库存提醒等后续版本。

### 14.3 本轮复盘

- 日期历史查询和聚合能力已可用，但最初的柱状趋势图在窄屏和长日期范围下占用过高并发生溢出。
- 最终采用项目既有的 Recharts 实现双折线图，更适合紧凑看板，也降低了手写图表布局的维护风险。
- 后续涉及图表或响应式布局的改动，除代码检查和构建外，必须增加真实移动端页面截图验证后再交付。
